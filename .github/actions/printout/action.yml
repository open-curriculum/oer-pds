# Printout
# prints inputs

name: Printout
description: Print workflow dispatch inputs for debugging
runs:
  using: 'composite'
  steps:
    - name: Print Inputs
      env:
        EVENT_INPUTS: ${{ toJSON(github.event.inputs) }}
      shell: bash
      run: |
        echo $EVENT_INPUTS